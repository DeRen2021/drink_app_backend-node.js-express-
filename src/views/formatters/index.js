const { formatLiquor, formatLiquors } = require('./liquorFormatters');
const { formatCocktail, formatCocktails } = require('./cocktailFormatter');
module.exports = {
  formatLiquor,
  formatLiquors,
  formatCocktail,
  formatCocktails
};